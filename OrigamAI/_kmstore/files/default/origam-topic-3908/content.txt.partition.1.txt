, DELETE is immediately reflected in those local files. 
 So when you shut down the container and restart it, all the data will be there because it’s physically stored on your machine in that folder. 
 
  


[Image: https://community.origam.com/uploads/default/original/2X/3/365c50d26f04beb2fa3431a2d250c4c5e4c82ea0.png]
Description: The image displays a Windows PowerShell terminal session. The title bar indicates the session is for Windows PowerShell, citing Microsoft Corporation. The terminal output shows a command to run a Docker container for Microsoft SQL Server, including the environment variables for acceptance of the End User License Agreement (EULA) and the SA password. The command includes parameters such as `--name mssql2022db` and `-p 1433:14` specifying the container name and port mappings. The Docker image is fetched from a URL: `mcr.microsoft.com/mssql/server:2022-latest`, and a unique image ID is provided. The command prompt returns to the user directory `C:\Users\victo>`.


  
 Now there is a Docker container with the MS SQL database server running in your local Docker. 
  


[Image: https://community.origam.com/uploads/default/original/2X/d/dc4fd02e7e7600f078684fb449ce9365d1aee6d0.png]
Description: The image displays the Docker Desktop interface, specifically the "Containers" section. It shows container CPU usage at 3.01% of a total of 1200% available, with 12 CPUs. The container memory usage is listed as 1.24GB out of 14.93GB. A search bar is present at the top, and a toggle for "Only show running containers" is visible. Below, there is a table with columns: Name, Container ID, Image, Port(s), CPU (%), Last started, and Actions, highlighting one container named "mssql2022db" with the Container ID "87608c13ca05," using image "mssql/server:2022-latest," operating on port 1433:1433, with a CPU usage of 1.87% and last started 22 seconds ago.


  
 If you plan to use this database server not just for training purposes but in production, consider also disabling the SA account as best security practice as described  here . 
 In case you are having any trouble, you can check  full instructions from Microsoft . 
   PostgreSQL 
 Running the PosgreSQL database system in a Docker container is easy. Below are scripts for both MacOS and Windows that create and run the database on your local machine. In both cases we create the user “postgres” with “MyPassword123” for later use. 
   MacOS 
 Here is a comprehensive script generated by Claude AI LLM for MacOS Terminal that creates a and starts a Alpine Linux based container with the PostgreSQL database system. 
 All you need to do is edit some of the information in the introductory Configuration section, such as the name of the container in Docker or the user password, and run it in Terminal. 
  #!/bin/bash

# Configuration
CONTAINER_NAME="PostgreSQL"
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="MyPassword123"
PORT="5432"
VOLUME_NAME="postgres_data"

echo "🚀 Starting PostgreSQL in Docker..."

# Check if Docker is running
if ! docker info > /dev/null 2>&1; then
    echo "❌ Docker is not running. Start Docker Desktop."
    exit 1
fi

# Stop (or remove) old container (if exists)
if [ "$(docker ps -aq -f name=$CONTAINER_NAME)" ]; then
    
    echo "❌ Docker container with that name already exists."
    exit 1

    # Or you can delete it if you prefer that
    # echo "🔄 Removing old container..."
    # docker rm -f $CONTAINER_NAME
fi

# Start new container
docker run -d \
    --name $CONTAINER_NAME \
    --restart unless-stopped \
    -e POSTGRES_PASSWORD=$POSTGRES_PASSWORD \
    -p $PORT:5432 \
    -v $VOLUME_NAME:/var/lib/postgresql/data \
    postgres:17-alpine

# Wait for database to start
echo "⏳ Waiting for database to start..."
sleep 5

# Health check
if docker exec $CONTAINER_NAME pg_isready -U $POSTGRES_USER > /dev/null 2>&1; then
    echo "✅ PostgreSQL is running!